/**
 * This is a reimplementation of the math.h
 * c library.
 */

#ifndef _MATH_H_
#define _MATH_H_

//get absolute value
#define ABS(x) (x >= 0 ? x : -x)
//get min
#define MIN(x, y) (x < y ? x : y)
//get max
#define MAX(x, y) (x > y ? x : y)

#endif //_MATH_H_
